import './App.css';
import {useReducer} from "react";

function App() {


    const reducer = (state, actions) => {

    };

  const init = () => {
    return
  };

  const useReducer1 = useReducer(reducer, 0, init);

    return (
        <div>

        </div>
    );
}

export default App;
